name: Keep Render Backend Alive

on:
  schedule:
    - cron: "*/5 * * * *"  # Runs every 5 minutes

jobs:
  ping_backend:
    runs-on: ubuntu-latest
    steps:
      - name: Send Request to Render Backend
        run: curl -s https://ionianems-backend.onrender.com/ping
